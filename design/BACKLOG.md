# Backlog

This file tracks real issues, deferred decisions, and follow-up work that should
not be lost.

## Open

- Decide whether `promptstrings` belongs in prompt assembly or remains an
  optional integration.
- Decide whether `agent-dashboard` belongs in TUI/live projection or remains an
  optional downstream pattern.
- Choose async test support strategy before adding async runtime code.
