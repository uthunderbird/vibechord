Human-only checks for this slice:
- Start one attached operation whose initial objective or success criteria are intentionally incomplete. While it is running or paused, enqueue `operator command <operation-id> --type patch_objective --text "..."` and `operator command <operation-id> --type patch_success_criteria --success-criterion "..." ...`, then resume if needed. Confirm `operator context`, `operator inspect`, and the next operator decision all reflect the patched persisted goal fields rather than only treating them as freeform advisory messages.
- Run `operator run --mode attached --involvement auto "Choose between two materially different repo strategies and proceed."` with a real provider, and confirm that when the model emits `requires_strategy_decision=true` on a non-terminal step the run stops before launching the next agent turn, records typed `novel_strategic_fork` attention, and shows the same fork coherently in `operator attention`, `operator context`, and `operator dashboard`.
- Run one real `operator run --project <profile> --involvement auto ...` scenario where the brain reaches a project-shaped decision without matching active policy and emits `metadata.requires_policy_decision=true`. Confirm the run blocks on typed `policy_gap` attention before any new agent turn is launched, and that `operator dashboard` / `operator context` show the same uncovered or no-policy coverage that triggered the guardrail.
- Record one policy with applicability filters, for example `operator policy record <operation-id> --title "Release approval gate" --text "Ask before production release steps." --category release --objective-keyword release --task-keyword deploy --agent codex_acp --run-mode attached --when-involvement auto`, then inspect it with `operator policy inspect <policy-id>` and confirm the stored applicability fields are explicit and bounded rather than being collapsed into freeform text.
- Start one operation whose persisted truth matches that policy and one that does not. Confirm `operator context <operation-id>` only shows the policy on the matching operation, and that the active-policy section explains both its applicability summary and why it matched.
- Run `operator project init <name> --cwd <repo-path> --agent codex_acp --harness "Use swarm when unclear."` in a real repo, then inspect the generated YAML and confirm it stays bounded and human-editable: only the requested stable defaults should be written, no empty sections should appear, and rerunning without `--force` should refuse to overwrite the file.
- Start or resume at least three operations with mixed states, for example one actively running attached operation, one paused operation, and one blocked on an attention request. Run `operator agenda` and confirm it groups them into `Needs attention` and `Active` in the expected order, with blocked or paused work shown before healthy active work.
- If those operations belong to different project profiles, run `operator agenda --project <profile-name>` and confirm the agenda only shows operations for that profile while preserving the same grouping and summaries.
- While an attached operation is actively waiting on a long-running agent turn, run `operator stop-turn <operation-id>` from another terminal. Confirm `watch` or attached live output shows `scheduler=draining` with a stopping summary, the active turn is cancelled without cancelling the whole operation, and the next operator move comes from replanning rather than silently treating the cancelled turn as normal success.
- While another attached operation is actively running, run `operator command <operation-id> --type stop_operation` from another terminal. Confirm the run terminates in `CANCELLED` with summary “Operation cancelled.”, the active turn receives cancellation, and `operator inspect/command` show the command as `applied` in persisted command history.
- Run `operator run --mode attached --involvement auto "Decide how this repo should handle manual-only verification and proceed accordingly."` in a project with no active testing policy. Confirm that if the operator needs a reusable rule, the resulting attention item is typed as `policy_gap` rather than a generic `question`, and that `operator attention <operation-id>` shows its context and suggested options clearly.
- Run `operator run --mode attached --involvement auto "Choose between two materially different release strategies for this repo."` in a setup where both routes are plausible. Confirm that a consequential route choice is surfaced as `novel_strategic_fork` when the operator cannot safely choose from current context alone, instead of silently improvising or emitting a vague generic clarification.
- While one of those runs is blocked, run `operator watch <operation-id>` or inspect the live attached output and confirm the surfaced attention label includes the typed prefix such as `[policy_gap]` or `[novel_strategic_fork]` instead of only a generic title.
- For either run above, confirm that `operator report <operation-id>` and `operator attention <operation-id>` tell a coherent story about why the operator stopped, including the typed attention title, question, and any suggested options.
- Add a local `operator-profile.yaml` with `default_objective` and run `operator run` with:
  - only local defaults and no CLI objective,
  - then again with `--project` and no `--success-criterion`,
  - then with CLI `--success-criterion` overrides.
  Verify `operator context` and `inspect` show the resolved profile metadata and that metadata overrides list
  marks only the actually changed fields (for example, explicit success criteria without objective drift).
- Run an attached operation with a real adapter, for example `operator run "Inspect this repo and report the top risk."`, and confirm the terminal now shows a live sequence of operator events while the run is active: start, brain decision, agent start/completion, evaluation, and final outcome. Confirm the output remains concise rather than dumping raw payloads.
- In a second terminal, run `operator watch <operation-id>` against an active attached run and confirm it replays the persisted event history, then continues live with concise state snapshots during quiet waits. Verify that attached truths such as the active session, waiting reason, and scheduler state changes like `pause_requested` or `paused` are visible without relying on the original `run` process output.
- After a run that creates at least one task artifact and memory entry, run `operator report <operation-id>` and confirm the report now includes durable work-state sections for tasks, memory, and artifacts rather than only timeline-style summaries.
- Run `operator inspect <operation-id> --json` and `operator report <operation-id> --json` for that same operation and confirm both payloads now include a `durable_truth` block with aggregated `tasks`, `memory`, and `artifacts`, so downstream tooling can consume the same long-lived truth the human report shows.
- After the run, inspect the same operation with `operator report <operation-id>`, `operator inspect <operation-id>`, `operator tasks <operation-id>`, `operator memory <operation-id>`, and `operator artifacts <operation-id>`. Confirm all five views tell a coherent story about what was done, what durable memory was distilled, and which artifacts were kept, without needing `--full` raw JSON.
- If the run produces more than one task or revises an existing task, confirm `operator tasks <operation-id>` surfaces the task statuses, task-specific memory refs, and artifact refs clearly enough to continue the work from persisted state alone.
- For an operation started from a project profile with at least one active policy entry, run `operator context <operation-id>` and `operator context <operation-id> --json`. Confirm both surfaces agree on the effective run mode, scheduler state, involvement level, resolved project profile values used at launch, policy scope, and active policy entries, without needing to reconstruct that control basis from separate `project`, `policy`, and `inspect` commands.
- Start or seed several operations with mixed states, then run `operator fleet --once` and `operator fleet` in a real terminal. Confirm the fleet dashboard groups operations from the same persisted agenda truth as `operator agenda`, updates live while operation states change, and shows suggested follow-up commands that point back to existing surfaces like `dashboard`, `watch`, `context`, `attention`, and `pause` rather than inventing hidden controls.
- In that same fleet run, confirm the header-level mix summaries stay honest while operations move between `running`, `blocked`, `paused`, and terminal states, and that degraded runtime-alert cases suggest `operator resume <operation-id>` instead of implying a pause or attention workflow.
- Run `operator project dashboard <profile-name> --once` and `operator project dashboard <profile-name>` for a project profile that already has active policy and mixed operation state. Confirm the dashboard combines resolved profile defaults, active project policy, and only the operations belonging to that project into one coherent workbench, without inventing project-only runtime state or showing operations from other profiles.
- With an active attached operation that has at least one task, one session, and at least one recent event, run `operator dashboard <operation-id>` in a real terminal. Confirm the live workbench updates while the run progresses, shows the current runtime/control context, tasks, sessions, attention, recent events, recent commands, and suggested control commands in one place, and exits automatically when the operation becomes terminal.
- For an operation backed by `codex_acp`, run `operator dashboard <operation-id> --codex-home <real-codex-home>` and confirm the `Codex Log` panel surfaces a condensed recent transcript view keyed from the persisted session id rather than duplicating the full upstream transcript.
- Run `operator dashboard <operation-id> --json` for the same operation and confirm the payload contains enough structured data for downstream tooling to consume the one-operation workbench state without scraping the human-rendered terminal layout.
- Run an attached operation through `claude_code`, then use `operator claude-log <operation-id>` and confirm the command resolves the persisted session automatically, shows a concise event stream rather than raw stream-json, and stays keyed by the operation id instead of requiring the user to dig for `.operator/claude/*.log` manually.
- For the same `claude_code` run, use `operator claude-log <operation-id> --follow` while the process is still active and confirm new assistant/tool/result events appear live as the underlying Claude log grows.
- Resolve a reusable attention item with `operator answer <operation-id> --attention <attention-id> --text "..." --promote --policy-objective-keyword release --policy-agent codex_acp`, then launch one matching run and one non-matching run in the same project. Confirm `operator context` shows the promoted policy only for the matching run, while `operator policy list --project <profile>` still shows the entry and its applicability summary for both.

Automated verification completed:
- `uv run ruff check src tests`
- `uv run pytest tests/test_runtime.py tests/test_cli.py tests/test_service.py`
